#include "json_converter.hpp"
#include <iostream>
int main(){

    JSON json;

    //json.parse("{ "key" : val, ..."); //convert text representation into JSON class instance
    //json["KEY"] = newValue            //Change some setting
    //std::string config = json.dump(); //make valid representation of the current config in string format

    return 0;
}